# 🔄 새 노트북 이전 가이드 — 투자 뉴스 브리핑 자동화

이 문서는 `daily_briefing` 예약 작업(매일 18:10 자동 실행)을 새 노트북에서 계속 돌리기 위한 설정 안내입니다.

## 핵심 개념

- **예약 작업 자체는 클라우드에서 실행되고 Claude 계정에 묶여 있습니다.** 새 노트북에서 같은 계정으로 로그인만 하면 작업이 자동으로 따라옵니다. 재등록 필요 없음.
- 이 노트북에 묶여 있던 것은 **연결된 폴더 하나뿐**입니다. 그 안에 GitHub 인증 토큰과 파일들이 들어 있습니다.
- 작업 스크립트는 실행 시 폴더 경로를 자동 탐지하므로, **폴더 이름만 `투자 뉴스 소식`으로 동일하면** 코드 수정 없이 작동합니다.

## 무엇을 옮겨야 하나

| 항목 | GitHub에 있음? | 이전 방법 |
|------|:---:|------|
| `briefing.html` (웹 뷰어) | ✅ | 저장소 clone으로 자동 복구 |
| 일자별 `*.md` 브리핑 히스토리 | ✅ | 저장소 clone으로 자동 복구 |
| `.github_token` (GitHub 인증 토큰) | ❌ | **이 노트북에서 직접 복사** (유일한 수동 단계) |

## 새 노트북 설정 순서

### 1. Claude 데스크톱 앱 설치 + 로그인
같은 Claude 계정으로 로그인하면 `daily_briefing` 예약 작업이 자동으로 나타납니다.

### 2. 프로젝트 폴더 생성 + 파일 복구
새 노트북에 폴더를 만듭니다. **이름은 반드시 `투자 뉴스 소식`** 으로 (예: `C:\Users\<사용자>\Claude\Projects\투자 뉴스 소식`).

그 폴더 안에서 GitHub 저장소 내용을 받습니다:
```
git clone https://github.com/Keemgunn99/investment-briefing.git .
```
→ `briefing.html` 과 히스토리 파일이 채워집니다.

### 3. GitHub 토큰 파일 복사 (가장 중요)
`.github_token` 파일은 GitHub에 올라가 있지 않으므로 직접 옮겨야 합니다.

- **이 노트북**의 `투자 뉴스 소식` 폴더에서 `.github_token` 파일을 찾습니다 (숨김 파일 — 탐색기에서 "숨긴 항목" 표시 켜기).
- USB / 클라우드 드라이브 / 비밀번호 관리자 등 **안전한 경로**로 새 노트북의 같은 폴더에 복사합니다.
- ⚠️ 토큰은 GitHub 저장소나 공개된 곳에 올리지 마세요.
- (토큰이 유출됐거나 분실하면 GitHub → Settings → Developer settings → Personal access tokens 에서 새로 발급 후 이 파일 내용을 교체)

### 4. Cowork에서 폴더 연결
새 노트북 Claude 앱에서 위 `투자 뉴스 소식` 폴더를 프로젝트 작업 폴더로 연결합니다.

### 5. 동작 확인
예약 작업을 수동으로 한 번 실행해보고, GitHub에 오늘 날짜로 커밋이 올라오는지 확인합니다.

## 참고
- 로컬 폴더 저장은 그 노트북의 Claude 앱이 켜져 있을 때만 됩니다. 단, RSS 수집 + GitHub push 는 클라우드에서 이뤄지므로, 브리핑 결과는 항상 GitHub 저장소에서 확인 가능합니다.
- 폴더 이름을 바꾸면 자동 경로 탐지가 실패하니 반드시 `투자 뉴스 소식` 유지.
